\* EXPECTED TO PASS.  The stated fix for the replay above, and the evidence
\* that it is sufficient rather than merely plausible.
\*
\* Identical to MC_Replay_N3M2.cfg in every respect --- the same earlier
\* run's signatures are in circulation, the same adversary may relay them
\* and call fulfillJob --- except that PriorJobId /= JobId.  That is
\* `submitJob` assigning a globally unique id (bridge.md 2, task 2.9f)
\* instead of coordinator.rs's `let job_id = 1;`.
\*
\* Every invariant holds, including the two that failed under replay.  The
\* replayed signatures now recover to addresses that are not their signers',
\* the registry check drops them, and the earlier run's attestations are
\* worth nothing here --- which is what "an attestation cannot be lifted onto
\* another job" was always supposed to mean.

INIT Init
NEXT Next

CONSTANTS
    Workers = {w1, w2, w3}
    Strangers = {}
    M = 2
    Results = {good, bad}
    TrueResult = good
    JobId = 1
    PriorJobId = 0
    FirstWriteWins = TRUE
    RefuseOnSplit = TRUE
    RegistryEnforced = TRUE
    RefundGuarded = TRUE
    AdversarialFulfill = TRUE
    ReplayAvailable = TRUE
    EquivocatingFaults = FALSE
    PatientDeadline = FALSE
    LateReplayOnly = FALSE
    MaxFaults = 1
    MinHonest = 0

INVARIANTS
    TypeOK
    QuorumIsReal
    SomeoneActuallyEvaluated
    EscrowPaidOnce
    NoSettleOnSplit
    ResultIsCorrect
    VoteNotDisplaced
