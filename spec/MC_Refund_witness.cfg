\* NON-VACUITY WITNESS.  EXPECTED TO FAIL.
\*
\* The refund half of EscrowPaidOnce: a trace in which refundOnTimeout
\* actually fires.  Without this, `fulfilCount + refundCount <= 1` could
\* hold because refunds never happen.

INIT Init
NEXT Next

CONSTANTS
    Workers = {w1, w2, w3}
    Strangers = {}
    M = 2
    Results = {good, bad}
    TrueResult = good
    JobId = 1
    PriorJobId = 1
    FirstWriteWins = TRUE
    RefuseOnSplit = TRUE
    RegistryEnforced = TRUE
    RefundGuarded = TRUE
    AdversarialFulfill = FALSE
    ReplayAvailable = FALSE
    EquivocatingFaults = FALSE
    PatientDeadline = FALSE
    LateReplayOnly = FALSE
    MaxFaults = 1
    MinHonest = 0

INVARIANTS
    NeverRefunds
