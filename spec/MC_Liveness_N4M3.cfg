\* LIVENESS.  EXPECTED TO PASS.  N = 4, M = 3, two faulty workers allowed
\* --- more than the tolerance N - M = 1, so many of these runs cannot
\* settle at all.  They still must not hang: the refund path closes them.

SPECIFICATION Spec

CONSTANTS
    Workers = {w1, w2, w3, w4}
    Strangers = {}
    M = 3
    Results = {good, bad}
    TrueResult = good
    JobId = 1
    PriorJobId = 1
    FirstWriteWins = TRUE
    RefuseOnSplit = TRUE
    RegistryEnforced = TRUE
    RefundGuarded = TRUE
    AdversarialFulfill = FALSE
    ReplayAvailable = FALSE
    EquivocatingFaults = FALSE
    PatientDeadline = FALSE
    LateReplayOnly = FALSE
    MaxFaults = 2
    MinHonest = 0

INVARIANTS
    TypeOK

PROPERTIES
    Liveness
